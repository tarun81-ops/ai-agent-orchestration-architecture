[pytest]
# Only the v1 test suite in tests\ should run. legacy_v1\ is a frozen backup that
# contains a copy of the same test files: without these two lines pytest finds two
# packages called "tests" and fails with "import file mismatch".
testpaths = tests
norecursedirs = legacy_v1 .venv build dist __pycache__ .pytest_cache
